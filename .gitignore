# Built ostree repo and upload bookkeeping
out/
.r2-synced

# Agent files are local and are not shared.
.claude/
.mcp.json

# Claude config dirs, if a session ever materialises one here.
.claude-amber/
.credentials.json

# wrangler writes the Cloudflare account id and name here.
.wrangler/
